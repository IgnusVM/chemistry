DATABASE_URL="postgresql://chemistry:chemistry@localhost:5432/chemistry?schema=public"

SESSION_SECRET="generate-with: node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\""

APP_BASE_URL="http://localhost:3000"

# SMTP for magic-link emails. Leave unset in dev to log links to the console instead of sending mail.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM="Chemistry <no-reply@chemistry.local>"
